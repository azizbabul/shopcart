<h1>Lists#index</h1>
<% @products.each do |product| %>
    <%= product.name %>
    </p>

    <p>
      <strong>Price:</strong>
      <%= product.price %>
    </p>
    <p>
      <strong>Description:</strong>
      <%= product.description %>
    </p>
    <p>
      <strong>image:</strong>
      <%=image_tag(product.image_url, :width =>100 , :height=> 100)%>
    </p>
<%end%>
<%= price %>